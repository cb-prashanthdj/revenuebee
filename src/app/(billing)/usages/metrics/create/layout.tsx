// app/home/usage/metric/layout.tsx (Metric layout - Override)
export default function MetricLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className=" ">
      <div className=""></div>
      {children}
    </div>
  );
}
